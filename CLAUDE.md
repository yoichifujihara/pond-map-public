# CLAUDE.md
# 石川県ため池マップ 公開版

## 1. プロジェクトの目的

本プロジェクトは、石川県公式Webサイトで一般公開するための
**シンプルで安全な農業用ため池位置情報ビューア**を作成することを目的とする。

一般利用者が、

- ため池の位置
- ため池名
- 市町名
- 所在地
- 基礎的なため池情報

を地図上で簡単に確認できることを目的とする。

詳細な防災解析、災害対応、DEM、等高線、水位解析、浸水シミュレーション等は、
別の非公開版で扱う。

本公開版には追加しない。


---

## 2. 最重要方針

以下を最優先する。

1. セキュリティ
2. 単純さ
3. 保守性
4. 軽量性
5. 自治体Webサイトへの導入容易性

機能の多さは優先しない。

「便利そうだから」という理由で機能を追加しないこと。

迷った場合は、**機能を追加するより削る方向を選ぶ。**


---

## 3. システム構成

完全な静的Webサイトとする。

基本構成は以下のみとする。

- HTML
- CSS
- JavaScript
- Leaflet
- JSON
- 国土地理院の背景地図タイル

以下は使用しない。

- PHP
- Python Webアプリ
- Node.jsサーバ
- データベース
- Firebase
- Cloudflare
- GitHub API
- CMS
- React
- Vue
- Angular
- npmを必要とする構成
- webpack
- Vite
- その他のビルドシステム

完成したファイル一式をWebサーバへコピーするだけで動作する構成とする。


---

## 4. データ

元データは、

`data/noto_ponds.json`

とする。

元データは原則として変更しない。

アプリケーション側で必要な整形を行うこと。

データ更新時は、

`data/noto_ponds.json`

を新しいファイルへ交換するだけで更新できる設計とする。

JSONの主要フィールドは以下。

- name
- city
- addr
- lat
- lon
- dam_h
- crest
- vol
- area

フィールドの意味や単位が不明な場合は推測して固定しない。

後から容易に変更できるようにする。


---

## 5. Leaflet

地図ライブラリはLeafletを使用する。

LeafletはCDNから読み込まない。

以下のようにローカルへ配置する。

`assets/vendor/leaflet/`

必要なJavaScript、CSS、画像をすべてサイト内に置く。


---

## 6. 背景地図

原則として国土地理院の淡色地図を使用する。

背景地図は第一版では1種類のみとする。

地図切替機能は実装しない。

国土地理院が求める出典表示を行う。

外部通信は原則として国土地理院の地図タイル配信先だけとする。


---

## 7. 公開版に必要な機能

必要な機能は原則として以下のみ。

### 地図表示

能登地域を中心として表示する。

可能であればJSON内の有効な緯度経度から表示範囲を自動計算する。

### ため池表示

JSON内のため池を地図上に表示する。

約1,500地点程度を想定する。

軽量性を優先し、LeafletのcircleMarker等を使用してよい。

必要に応じてCanvasレンダリングを使用する。

### ため池情報表示

ため池をクリックすると基礎情報を表示する。

最低限、

- ため池名
- 市町
- 所在地

を表示する。

### 市町検索

市町を選択して絞り込めるようにする。

### ため池名検索

ため池名の部分一致検索を行えるようにする。

### 検索解除

検索条件を初期化できるようにする。

### レスポンシブ対応

PCおよびスマートフォンで最低限利用できるようにする。


---

## 8. 原則として実装しない機能

以下は公開版には実装しない。

- 浸水想定区域
- ハザードマップ
- DEM
- 等高線
- 水位変更
- 浸水シミュレーション
- 災害前後比較
- 航空写真比較
- 外部レイヤ追加
- GPS
- 現在位置取得
- 写真投稿
- ファイルアップロード
- コメント
- ログイン
- ユーザー管理
- Cookie
- LocalStorage
- Google Analytics
- アクセス解析
- SNS連携
- 外部API検索
- ジオコーディング
- ルート検索
- PWA
- Service Worker

これらを追加する必要が生じた場合は、勝手に実装せず、まず必要性を整理すること。


---

## 9. セキュリティ

自治体公式サイトでの公開を前提とする。

以下は禁止する。

- eval()
- new Function()
- document.write()
- JSONP
- 不要な外部JavaScript
- 不要な外部CSS
- 任意URLへのfetch
- URLパラメータを利用した外部リソース読み込み

JSON値を画面に表示する際は、
可能な限り以下を使用する。

- document.createElement()
- textContent

JSON内の文字列をinnerHTMLへ直接挿入しない。

LeafletのPopupについても、
文字列連結したHTMLではなくDOM要素を生成して渡す方式を優先する。

APIキー、トークン、パスワード、秘密情報をコード内に含めない。


---

## 10. データ検証

JSON読み込み時に最低限以下を確認する。

- latが数値である
- lonが数値である
- 緯度が妥当な範囲である
- 経度が妥当な範囲である

不正な地点が存在しても、
アプリケーション全体を停止させない。

不正なレコードのみスキップする。


---

## 11. 外部依存

外部依存は最小限とする。

外部CDNは禁止する。

Google Fonts等も使用しない。

フォントはOS標準フォントを使用する。

追加ライブラリは原則として導入しない。

新しいライブラリを追加する場合は、
Leafletだけでは実現できない明確な理由があるか確認する。


---

## 12. UI設計

自治体公式サイトに掲載して違和感のない、
シンプルなデザインとする。

基本方針：

- 白背景
- 薄いグレー
- 落ち着いた配色
- 十分な文字サイズ
- 不要な装飾なし
- 不要なアニメーションなし

地図が主役となる構成にする。


---

## 13. アクセシビリティ

最低限以下を考慮する。

- HTMLのlang="ja"
- 適切なtitle
- form要素へのlabel
- button要素を正しく使用する
- キーボード操作を妨げない
- 色だけで情報を区別しない
- 十分な文字サイズとコントラスト


---

## 14. ファイル構成

できるだけ単純な構成とする。

基本形：

```text
ishikawa-tameike-map-public/
├─ CLAUDE.md
├─ index.html
├─ README.txt
├─ SECURITY.md
├─ data/
│  └─ noto_ponds.json
├─ assets/
│  ├─ css/
│  │  └─ style.css
│  ├─ js/
│  │  └─ app.js
│  └─ vendor/
│     └─ leaflet/
└─ LICENSES/
   └─ leaflet-license.txt
```

必要性のないファイルは増やさない。


---

## 15. README.txt

README.txtは県担当者が読むものとする。

技術者でなくても理解できる文章とする。

最低限以下を書く。

- このサイトの目的
- 公開方法
- サーバ要件
- 外部通信先
- データ更新方法
- ファイル構成
- 簡単な動作確認方法


---

## 16. SECURITY.md

SECURITY.mdは県の情報システム・セキュリティ担当者への説明を目的とする。

最低限以下を書く。

- 静的Webサイトである
- サーバ側プログラムなし
- データベースなし
- ログインなし
- Cookieなし
- LocalStorageなし
- 利用者情報収集なし
- 外部JavaScriptなし
- 外部CSSなし
- Leafletはローカル配置
- JSONは同一サイト内
- 外部通信先
- eval等を使用していない
- APIキーや秘密情報なし


---

## 17. 開発時の原則

変更前に既存ファイルを確認する。

正常に動作している機能を壊さない。

必要以上のリファクタリングをしない。

今回の目的と関係のない改善を勝手に行わない。

コードを複雑化しない。

同じ処理を過剰に抽象化しない。

小規模な静的サイトとして理解しやすいコードを優先する。


---

## 18. 動作確認

変更後は必ず動作確認を行う。

最低限確認すること：

- ページが正常に開く
- JSONが読み込める
- 地図が表示される
- 全ため池が表示できる
- ポップアップが表示される
- 市町検索が動作する
- ため池名検索が動作する
- 検索解除が動作する
- JavaScriptエラーがない
- スマートフォン幅でも操作可能
- 不要な外部通信がない


---

## 19. 完成条件

最終成果物は、

**ZIPを県担当者へ送り、解凍した内容をWebサーバへ配置するだけで公開できる**

状態とする。

以下を県側へ要求する構成にしてはいけない。

- npm install
- npm build
- Python環境
- Node.js環境
- PHP環境
- DB作成
- APIキー設定
- クラウドサービス契約

「静的ファイルを配置するだけ」を最優先する。